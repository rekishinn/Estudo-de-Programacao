function somarNumeros(a, b) {
    //var a = 5;
    //var b = 10;

    var soma = a + b;
    return soma;
    }
//poderá ser executada em outro arquivo, pois o index.html lê primeiro as funcoes
//(e sim, a ordem importa lá no codigo) e depois lê o script, tudo em conjunto.